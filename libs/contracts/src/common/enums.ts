export enum UserRole {
    CUSTOMER = 'CUSTOMER',
    CHEF = 'CHEF',
    ADMIN = 'ADMIN',
}

export enum OtpPurpose {
    LOGIN = 'LOGIN',
}

export enum DevicePlatform {
    IOS = 'IOS',
    ANDROID = 'ANDROID',
    WEB = 'WEB',
}
