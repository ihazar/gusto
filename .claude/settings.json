{
    "hooks": {
        "PostToolUse": [
            {
                "matcher": "Edit|Write|MultiEdit",
                "hooks": [
                    {
                        "type": "command",
                        "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in \"$PWD\"/*) npx prettier --write --ignore-unknown \"$f\" >/dev/null 2>&1 || true ;; esac; }",
                        "statusMessage": "Formatting with Prettier…"
                    }
                ]
            }
        ]
    }
}
